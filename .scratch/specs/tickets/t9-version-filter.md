## Parent

#32（spec: 版本/赛季趋势——版本筛选与赛季报告）

## What to build

车队成员在榜单/成员卡查询时可以按版本筛选（如"15.16 版本内我的表现"），版本筛选与既有筛选（日期区间、维度等）叠加生效。game_version 原始值到主版本的归一函数全站单点定义（本票必须钉死的唯一决策，黄金样本测试覆盖）。

这是对既有查询接口的参数扩展，不新建平行接口；前端版本筛选控件融入既有查询控件。赛季报告是下一张票。

## Acceptance criteria

- [ ] game_version 归一函数（原始格式 → 主版本）全站单点定义，有黄金样本单测
- [ ] 既有榜单/成员卡查询接口新增版本筛选参数，与日期区间等既有筛选叠加生效
- [ ] 前端查询控件支持版本选择
- [ ] 不新建平行接口，纯参数扩展
- [ ] controller 只做参数校验与返回封装
- [ ] TDD：归一函数黄金样本单测 + 版本筛选（含叠加）测试先行，先例为既有榜单/成员卡查询参数测试
- [ ] 无效版本参数走既有参数错误码（1xxx）语义

## Blocked by

None (can start immediately).
