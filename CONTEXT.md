# League Akari Server

英雄联盟对局同步后端，提供对局数据同步、存储与查询 API。

## Language

**MVP（Most Valuable Player）**：
胜方中按职业差异化评分体系计算得分最高的选手，每场对局至多 1 名。
_Avoid_：全场最佳、击杀王

**SVP（Second Valuable Player）**：
负方中按职业差异化评分体系计算得分最高的选手，每场对局至多 1 名。
落库与接口中的字段值是 `ACE`（`match_mvp.type`），语义即 SVP——沿用历史命名，不要新造第三种叫法。
_Avoid_：败方MVP、输方最佳

**腾讯侧 puuid（Tencent UUID）**：
腾讯运营区服（台服 TW2 等）客户端经 LCU/SGP 上报的玩家唯一标识，格式为带连字符的 UUID（如 `3e242ccb-b520-5f29-8551-a7ad71b8f629`）。
_Riot 全局 puuid 是另一套标识符，两者不可互换、不可互相推导，详见成员身份集合。_

**Riot 全局 puuid（Riot PUUID）**：
Riot 官方 API（Account-V1 / MATCH-V5）使用的玩家唯一标识，格式为无连字符长串（如 `IZOp3JUSyJct...`）。
MATCH-V5 历史回填只能按此标识查询。
_Avoid_：与腾讯侧 puuid 混用；跨体系做等值匹配

**成员身份集合（Member Identity Set）**：
一名玩家全部已知标识符的并集（腾讯侧 puuid ∪ Riot 全局 puuid），由 `team.roster` 配置的"昵称#tag"经库内反查与 Riot API 解析而来。车队相关的所有匹配（周报/榜单/成员卡/名场面）必须按身份集合进行，跨数据源的聚合按成员（riotId）为键，而不是按单个 puuid。
_Retry 提示：新增玩家匹配逻辑时，先确认是否可能横跨两种 puuid 体系。_

**车队对局（Fleet Game）**：
同局出现的车队成员数 ≥ 配置阈值（`team.min-shared-members`，默认 2）的对局，用于过滤成员的单人局/路人局。周报与全部榜单只统计车队对局；成员卡（个人视角）统计个人全部对局。

**一局摘要（Fleet Game Summary）**：
车队视角的单局全量事实中间表示：胜负、比分（双方击杀合计）、队列/时长、主队判定（车队成员多数所在队）、双方全员行（车队成员置前 + 击杀降序，含 KDA/伤害/承伤/金币/称号）。战报图与局后锐评是它的两个投影，口径只存在这一处。
_Avoid_：在投影层各自重新判定主队/重算比分/重排阵容（曾导致战报图比分恒显 0:0 的口径漂移 bug）_

**英雄职业（Champion Class）**：
英雄的固有职业分类，与对局中的位置（分路）无关。包括射手、法师、坦克、刺客、战士、辅助六个类别，用于确定评分维度权重。

**同队归一化（Team-Normalized Score）**：
某个评分维度在本队 5 名选手内做 0-100 线性归一化，最高者得 100，最低者得 0，消除不同职业间原始指标量级差异。

**评分维度（Scoring Dimension）**：
构成总分的单个评价维度，如"伤害输出"、"承伤能力"、"视野贡献"、"控制时长"、"经济转化"等。每个维度包含原始值（raw）和归一化得分（score）。

**职业差异化评分（Role-Adaptive Scoring）**：
根据英雄职业动态调整各评分维度的权重，使评分更贴合该职业在对局中的核心职责。例如坦克的承伤权重大于射手的承伤权重。

**大乱斗修正（ARAM Adjustment）**：
在大乱斗系队列（极地大乱斗 450 / 海克斯乱斗 2400、2410、2450）下，辅助职业的视野维度不参与职业差异化评分（权重视为 0），以适应大乱斗地图无草丛/无插眼的玩法特点。
_Avoid_：按 gameMode 字符串判定大乱斗（LCU 的 CHERRY 实为斗魂竞技场，曾导致旧引擎判定永不命中）_

**车队群（Fleet Group）**：
局后播报的推送目标 QQ 群，与车队 1:1 绑定，群的 `group_openid`（官方开放平台下发，非群号）在配置 `push.group-open-id` 声明。
_Avoid_：把 QQ 群号当 openid 使用；一个群绑定多个车队

**局后播报（Post-Game Broadcast）**：
车队对局**首次入库**且被判定为"刚结束"（落库时刻与估算的对局结束时刻之差在时间窗内）时，服务端自动向车队群推送本局战报图与局后锐评的自动化行为。历史回填（backfill）与明显旧局不触发；触发与送达不依赖任何人工操作。
_Retry 提示：实时推送失败的局会由桌面端轮询补推再次入库，"首次入库"与"时间窗"双条件保证同一局只播报一次且补推兜底可达。_

**英雄过滤（Champion Filter）**：
对局列表查询的可选筛选维度，语义为"查询者本局**使用的英雄**"——`champion_id` 条件加在查询者本人的参与者行上（与 puuid/summonerName 同一条定位查询），过滤结果是"该玩家用过这个英雄的对局"，而非"出现过这个英雄的对局"。与队列/时间筛选叠加生效。
_Avoid_：把条件加在对局主表或全量参与者批量加载上（前者无从过滤，后者会滤出"对手用过该英雄"的对局，语义漂移）_

**战报图（Match Report Card）**：
服务端 Java2D 渲染的对局战报 PNG（900px 宽社交卡片视觉），内容含胜负比分、资源对比（塔/龙/大龙/一血）、双方阵容与全员"输出占比/承伤占比/伤害转化"三指标。是局后播报的第一条消息，仅含数据、不依赖 AI。
_Avoid_：把 AI 文案渲染进战报图（会导致图片延迟与 AI 故障耦合）

**局后锐评（Post-Game AI Comment）**：
车队视角的单局 AI 点评文本（约 200-300 字，以 Markdown 消息发群，重点词加粗醒目），在战报图之后作为第二条消息发送。输入基于**双方 10 人全量数据**（伤害/承伤/经济/称号），火力全开点名开团，仅"火力对局不对人"一条底线。区别于单局 AI 分析（self 视角、SSE 流式）与周报锐评（按周聚合）；生成失败自动重试 2 次，全部失败则改发"AI 缺席提示"。
_Avoid_：复用单局 AI 分析的 self 视角提示词；把锐评生成做成流式

**周报锐评（Weekly AI Comment）**：
按周聚合的车队 AI 点评，是周报（总览 + 七榜单 + 名场面）之外单独流式呈现的部分。
已结束的周内容不可变：首次生成后完整文本持久化落库，后续请求直接返回不再调 AI；
当前周（进行中）用短缓存，且支持强制刷新（跳过缓存重新生成，60 秒冷却兜底，
决策见 ADR 0010）；对历史周传强制刷新参数被忽略（不可变原则优先）。
_Avoid_：把"历史周持久化"策略套用到单局 AI 分析（对局详情会因时间线补同步而变化，两者缓存语义不同）；给历史周提供重新生成能力_

**时间线复盘（Timeline Replay）**：
Web 对局详情页的复盘区块：从时间线 frames 提取经济差曲线、击杀事件标记与关键转折点，
AI 叙述按需手动触发（SSE 流式，与单局 AI 分析同一事件契约）。仅对有时间线数据的
对局可用（桌面端实时采集，或经回填补拉补齐）；无时间线的对局优雅降级，不渲染曲线。
_Avoid_：在无时间线的对局上渲染空白曲线图；把复盘做成 QQ 推送（交互式曲线不适合静态图）_

**关键转折点（Turning Point）**：
规则引擎从时间线 frames **确定性提取**的对局势能变化节点（经济差极值、一血、团灭、
大龙、经济反超时刻等），每个点携带真实上下文数据（时刻、双方经济差、涉及成员与事件）。
AI 叙述只消费转折点做解读，不负责发现事件。
_Avoid_：让 AI 从原始 frames 自行"发现"转折点（无法复算、可能编造不存在的事件）_

**对局诊断（Game Diagnosis）**：
对局详情页的纯数据诊断区块，基于 stats_json 中尚未被评分体系使用的字段（视野得分、
控制时长、经济分配、资源控制等）回答"这局短板在哪"，败局自动高亮短板维度。
与战报图三指标（输出占比/承伤占比/伤害转化）不重叠，与时间线复盘互补（复盘看过程，
诊断看结果归因）。不依赖 AI。
_Avoid_：与战报图既有三指标重复建设；把诊断维度混入 OP Score 评分（评分体系版本已冻结在 v4）_

**搭档胜率矩阵（Duo Win-Rate Matrix）**：
车队成员两两搭档的车队对局胜率矩阵（N×N，N 为成员数），每格展示胜率与**局数**。
样本量小的格子以局数标注而非裸百分比呈现，避免"2 局 100% 胜率"式误导。
归属榜单中心的"组合"tab，只统计车队对局。

**赛季报告（Season Report）**：
赛季维度的车队总结大页面（版本胜率曲线、英雄池随版本漂移、高光时刻等），
**手动触发**生成，不做定时自动生成（台服赛季边界判定需人工确认更稳）。
平时版本趋势以筛选维度形式融入现有查询（榜单/成员卡按 game_version 过滤）。
与「赛季资料」是两个独立投影，互不吸收：本词只指 `/season-report` 那个页面。

**赛季（Season）**：
本项目内一切"按赛季聚合"一律以**游戏版本**为断代轴——`match.game_version` 经 `GameVersionNormalizer` 归一后的主次版本（如 `16.15`），页面上直接显示版本号。
不存在独立赛季目录（无赛季表、无赛季 API）：Riot 不提供赛季边界接口，台服/国服/各模式的赛段边界无法自动判定，硬编码日期必然漂移。
_Avoid_：用官方排位赛季（S15 等）做断代轴；把榜单中心"本赛季"当成真赛季（其实实现为当年 1 月 1 日至今）；用原始四段版本号（`16.15.802.4387`）当分组键_

**出场率（Appearance Rate）**：
海克斯强化统计的口径：某强化在**某英雄对局**中被玩家持有的次数 ÷ 该英雄对局数。
分子取 `stats_json` 的 `playerAugment1-6` 中每个非零 ID（一局最多贡献 6 次），故同页所有强化的出场率之和远大于 100%。
它**不是**选取率：三选一中被放弃的候选不留任何痕迹，候选集无法从现有数据还原。
_Avoid_：把出场率标成"选取率"（读者会按 100% 去读）；拿对局数当分子_

**海克斯乱斗对局（KIWI Game）**：
海克斯强化统计的样本范围：`queue_id ∈ {2400, 2410, 2450}` **且**该参与者行 `playerAugment1-6` 至少一个非零——两条同时满足才纳入。
双条件并列是为了让"有强化但被白名单排除的局数"成为可观测的覆盖率指标：白名单选错时页头口径行显示该计数，而不是安静地全空。
_Avoid_：并入 450 极地大乱斗（无强化局虚增分母、稀释全部出场率）；并入 1700/1710 斗魂竞技场（2 人小队、名次制胜负，与推家胜负不可混算）_

**全库参与者行（All-Participant Rows）**：
英雄×强化统计的取行范围：全库海克斯乱斗对局的**全部参与者行**（含敌方、含路人），不按车队成员身份集合过滤、不按车队对局过滤（决策见 ADR 0014）。
理由是样本厚度：统计单元是"英雄×强化"，切到具体组合后样本极薄，全库是唯一撑得起格子的口径；强化强度本不分敌我。
_Avoid_：套用成员卡口径（本人全部对局）或七榜口径（车队对局）——两者都会丢掉七成以上可用行

**强化胜率（Augment Win Rate）**：
某英雄×某强化的胜率 = 持有该强化的**胜场数** ÷ 持有该强化的**对局数**。
分母刻意与出场率不同（出场率分母 = 该英雄对局数），两列数字不同源，防止读者混读。
同一参与者行内重复出现的强化 ID 只计 1 次（防御性去重）。
_Avoid_：拿该英雄对局数当胜率分母（语义变成"贡献了多少胜利"，数值被未持有局稀释）；与出场率共用分母_

**AI 摘要加厚（AI Summary Enrichment）**：
四个 AI 场景（战犯出列/周报锐评/局后锐评/复盘叙述）喂给模型的输入摘要以"完整信息"
为准——敌方对比基准、治疗/护盾/资源伤害、占比分母等分析原料尽量齐全（决策见
ADR 0009）。摘要字段用短键 + 中文名转换（英雄外号规则见各提示词文件），数字 ID
经 GameDataService 转换后才入摘要。
_Avoid_：为省首 token 延迟砍分析原料（质量优先）；喂原始 stats_json 全量（噪音大体积失控）_

**复盘文风（Replay Narration Voice）**：
复盘叙述保持教练团队视角（ADR 0008 的"团队复盘不点名毒舌"），但英雄称呼用玩家
常用外号、语言犀利直接——粒度对齐战犯出列，立场区别于战犯出列。
_Avoid_：把复盘写成虎扑点名开团（那是局后锐评的活）_

**敌方情报推送（Pre-Game Intel Broadcast）**：
车队对局**游戏真正开始**（Gameflow 进入 InProgress）时，服务端自动向车队群推送
敌方情报卡的自动化行为。与局后播报对应：一个管"进局前侦察"，一个管"局后战报"。
选人秒退/秒锁退的对局不推（推送锚点是游戏开始，不是选人开始）。
_Avoid_：以选人 session 为去重锚点（同一局可能进两次选人）；把推送时机提前到选人开始（此时情报未必算完）_

**敌方情报卡（Enemy Intel Card）**：
选人阶段敌方侦察的可视化结果，服务端 Java2D 渲染的 900px PNG（复用战报图渲染管线与富媒体上传链路）。
内容：标题（车队名 + 红蓝方 + 队列）+ 敌方 5 人行（召唤师名/段位/近 20 局窗口胜率）+
开黑分组视觉 + 各组搭档胜率。纯数据、不依赖 AI。段位或历史缺失的玩家如实标注"未知"，不隐藏。
_Avoid_：把我方 5 人放进卡里（我方是谁群友在客户端里自己看得见）；小样本胜率裸百分比_

**敌方侦察数据（Enemy Scout Data）**：
桌面端在选人阶段经 SGP 通道（登录态、近乎无频控）拉取并喂给后端的敌方 match 摘要：
每局的 participants（puuid/teamId/胜负/召唤师名）+ queueId。是开黑判定与窗口胜率的
唯一数据原料；后端不自己调 Riot API 拉陌生人历史（限流下 2-3 分钟起步，选人窗口等不起，
决策见 ADR 0011）。轻量落库作侦察缓存，再遇同一批人时命中缓存秒出卡。
_Avoid_：让后端经 Riot API 硬拉敌方历史；落库完整对局详情（只存共现判定所需的摘要行）_

**开黑判定口径（Premade Detection Criteria）**：
敌方情报推送判定"谁是开黑组"的规则：从历史对局提取"谁和谁同队"，共现次数 ≥ 阈值
（后端可配）即有开黑关系，再做子集剔除与重叠合并。与桌面端 ongoing-game 的推断逻辑
同口径移植（Java 版），保证桌面端 UI 与群里情报卡不出现矛盾结论。
_Avoid_：在判定阶段引入胜率权重（胜率是判定结果上的展示信息，不是判据）_

**窗口胜率（Window Win Rate）**：
敌方玩家在喂进来的 20 局历史窗口内的原始胜率（胜场/局数），不做队列过滤。
与搭档胜率矩阵的小样本原则一致：局数一并展示，不裸百分比。
_Avoid_：按队列过滤窗口（Q8 决策：零额外请求、口径简单优先）_

**我方锚定（Friendly Anchor）**：
敌方情报推送判定"哪边是我方"的规则：选人数据中车队 roster 成员（按成员身份集合匹配）
多数所在的队为我方，teamId 100 = 蓝色方、200 = 红色方。触发上报的桌面端身份仅作参考信号，
冲突时以 roster 多数派为准（复用车队对局的主队判定语义）。
_Avoid_：以上报者个人所在队为锚（上报者未必是车队成员）_

**时间线采集时序（Timeline Sync Ordering）**：
桌面端单场同步的完成语义：对局详情（match）与时间线（timeline）**都推送成功**才算
"已处理"——去重标记在时间线成功后才写入。任何一步失败（网络抖动、EndOfGame 时
frames 尚未生成）都不标记，由 2 分钟定时同步自动补推（match 重推由后端 game_id
幂等兜底，无副作用）。
_Avoid_：match 推送成功就标记去重（时间线会被静默跳过 → 复盘面板"暂未采集到时间线数据"）_

**统一采样参数（Unified Sampling Params）**：
四个 AI 场景（战犯出列/复盘叙述/周报锐评/局后锐评）共用的模型调用参数集：
统一 `ai.model` + `ai.max-tokens` + `ai.thinking`（+ 可选 `ai.thinking-effort`），
不存在场景级模型/上限键（ADR 0012）。
_Avoid_：无真实差异化需求时新增场景级键（漂移温床：三键从未分叉却漂移成两种值）_
